# ── Required in production ──
# Secret used to sign session tokens (HMAC-SHA256). The app refuses to start
# signing sessions in production if this is unset. Generate one with:
#   openssl rand -hex 32
AUTH_SECRET=
